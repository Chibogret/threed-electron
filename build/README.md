# Build resources

Place platform-specific installer assets in this directory when packaging the app.

Recommended files:
- `icon.ico` for Windows installers
- `icon.icns` for macOS builds

These files are optional. If omitted, the default Electron icons will be used during packaging.
